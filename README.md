# 🚌 Bus Tracker - Setup & Usage Guide

## 📌 Overview

The Bus Tracker app allows users to track nearby buses in real-time using **Google Maps** and **Firebase Realtime Database**.  
It also **sends notifications** when a bus is near (within 1km) and **handles location permission errors gracefully**.

---

## 📂 Project Setup

### 1️⃣ Clone the Project

To get the project on your local machine, run:

```sh
git clone https://github.com/your-repository/bus_tracker.git
cd bus_tracker
```

### 2️⃣ Install Dependencies

Navigate to the project folder and install dependencies:

```sh
flutter pub get
```
